<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phaxtrack">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:launchMode="standard"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".otp"></activity>
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChooseLogin" />
        <activity android:name=".patient_vaccine_button" />
        <activity android:name=".patient_adverse_button" />
        <activity android:name=".patient_information_button" />
        <activity
            android:name=".patientEdit"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".PatientInfo" />
        <activity android:name=".registerPatient" />
        <activity android:name=".dashboard.doctor" />
        <activity android:name=".dashboard.institution" />
        <activity android:name=".dashboard.vaccine" />
        <activity android:name=".adminDashboard" /> <!-- <activity -->
        <!-- android:name=".SampleActivity" -->
        <!-- android:label="@string/title_activity_sample" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" > -->
        <!-- </activity> -->
        <activity android:name=".dashboard.registerDoctor" />
        <activity android:name=".adminLogin" />
        <activity android:name=".PatientLoginPage" />
        <activity android:name=".DoctorLoginPage" />
        <activity android:name=".index" />
    </application>

</manifest>